import { IntroPage } from '@/components/intro-page';

export const metadata = {
  title: 'Iniciar | SUPERSAPIENS',
  description: 'Inicie sua jornada de transformação com o SUPERSAPIENS.',
};

export default function IniciarPage() {
  return <IntroPage />;
}
